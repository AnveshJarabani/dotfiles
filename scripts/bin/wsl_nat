#!/usr/bin/env bash
# Start WSL NAT PowerShell script with elevated privileges

WIN_USER=$(pass windows/username)
powershell.exe -Command "Start-Process powershell.exe -ArgumentList '-File \"C:\Users\\$WIN_USER\SCRIPTS\wsl_nat.ps1\"' -Verb RunAs"
